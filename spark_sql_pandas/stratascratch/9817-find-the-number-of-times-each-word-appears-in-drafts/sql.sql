SELECT
	WORDS.WORD,
	COUNT(*) AS NENTRY
FROM
	(
		SELECT
			TRIM(
				BOTH '.,'
				FROM
					UNNEST(STRING_TO_ARRAY(CONTENTS, ' '))
			) AS WORD
		FROM
			GOOGLE_FILE_STORE
		WHERE
			FILENAME LIKE 'draft%'
	) WORDS
GROUP BY
	WORDS.WORD