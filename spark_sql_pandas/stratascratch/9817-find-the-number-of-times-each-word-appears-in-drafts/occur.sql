SELECT
	WORDS.WORD,
	COUNT(*) AS NENTRY
FROM
	(
		SELECT
			TRIM(
				BOTH '.,'
				FROM
					UNNEST(STRING_TO_ARRAY(CONTENTS, ' '))
			) AS WORD
		FROM
			GOOGLE_FILE_STORE
	) WORDS
WHERE
	WORD IN ('bull', 'bear')
GROUP BY
	WORDS.WORD