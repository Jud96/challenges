SELECT
	CATEGORY,
	SUM(REVIEW_COUNT) AS REVIEW_CNT
FROM
	(
		SELECT
			UNNEST(STRING_TO_ARRAY(CATEGORIES, ';')) AS CATEGORY,
			REVIEW_COUNT
		FROM
			YELP_BUSINESS
	) T
GROUP BY
	CATEGORY
ORDER BY
	2 DESC